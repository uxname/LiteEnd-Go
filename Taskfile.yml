version: "3"

vars:
  BIN: bin
  PKG: github.com/uxname/liteend-go
  # renovate: datasource=go depName=github.com/fe3dback/go-arch-lint
  GO_ARCH_LINT_VERSION: v1.18.0
  # renovate: datasource=go depName=golang.org/x/tools
  X_TOOLS_VERSION: v0.49.0
  # renovate: datasource=go depName=github.com/vladopajic/go-test-coverage/v2
  GO_TEST_COVERAGE_VERSION: v2.19.0

tasks:
  default:
    cmds:
      - task --list
    silent: true

  setup:
    desc: One-command onboarding for a fresh clone (env, hooks, codegen, deps DB)
    cmds:
      - cmd: cp -n .env.example .env || true
        silent: true
      - cmd: go build -o {{if eq OS "windows"}}NUL{{else}}/dev/null{{end}} github.com/bokwoon95/wgo || true
        silent: true
      - lefthook install || echo "lefthook not installed; run 'go install github.com/evilmartians/lefthook@latest'"
      - task: gen
      - docker compose up -d db redis garage garage-init
      - echo "Setup done. Start the app with hot-reload via 'task start:dev'."

  start:dev:
    desc: Run the server locally with hot-reload (wgo) against the compose DB+Redis
    cmds:
      - docker compose up -d db redis garage garage-init
      - go tool github.com/bokwoon95/wgo run ./cmd/server

  start:prod:
    desc: Run the server locally (no hot-reload, production-like)
    cmds:
      - go run ./cmd/server

  gen:
    desc: Run all code generators (sqlc, gqlgen) and format the output
    cmds:
      - task: gen:sqlc
      - task: gen:gqlgen
      - task: fmt

  gen:sqlc:
    desc: Generate type-safe DB access from db/queries
    cmds:
      - go tool github.com/sqlc-dev/sqlc/cmd/sqlc generate

  gen:gqlgen:
    desc: Generate GraphQL server code from schema
    cmds:
      - go tool github.com/99designs/gqlgen generate

  gen:check:
    desc: Fail if generated code (sqlc + gqlgen) is out of sync with the schema/queries
    cmds:
      # Regenerate, format only the generated output, then diff just those paths
      # so the check is robust on a dirty working tree (e.g. inside a git hook).
      - go tool github.com/sqlc-dev/sqlc/cmd/sqlc generate
      - go tool github.com/99designs/gqlgen generate
      # gqlgen also rewrites the resolver dir, so format it too (its hand-written
      # bodies are out of the diff scope below).
      - go tool github.com/golangci/golangci-lint/v2/cmd/golangci-lint fmt internal/db/sqlc internal/graph/generated internal/graph/model internal/graph/resolver
      - go tool mvdan.cc/gofumpt -w internal/db/sqlc internal/graph/generated internal/graph/model internal/graph/resolver
      - git diff --exit-code -- internal/db/sqlc internal/graph/generated internal/graph/model/models_gen.go || (echo "Generated code is stale — run 'task gen' and commit" && exit 1)

  check:
    desc: Full project check (codegen, tidy, lint, arch, deadcode, vuln, secrets) — run before committing
    cmds:
      - task: gen:check
      - task: tidy:check
      - task: compose:check
      - task: lint
      - task: arch
      - task: deadcode
      - task: vuln
      - task: secrets

  compose:check:
    desc: Validate compose files render (schema, interpolation, :? guards)
    cmds:
      # Dummy values satisfy BOTH files' required-var guards, so this is
      # hermetic — neither compose file needs a .env any more. A YAML typo, bad
      # interpolation or a NEW :? guard without a matching dummy fails here —
      # before any deploy.
      # Skipped entirely when docker is absent, mirroring the secrets task.
      - cmd: |
          if ! command -v docker >/dev/null 2>&1; then
            echo "docker not installed — skipping compose check"
            exit 0
          fi
          for f in docker-compose.prod.yml docker-compose.yml; do
            env CORS_ORIGIN=dummy DATABASE_PASSWORD=dummy OIDC_ISSUER=dummy OIDC_AUDIENCE=dummy OIDC_JWKS_URI=dummy \
              S3_ENDPOINT=dummy S3_ACCESS_KEY_ID=dummy S3_SECRET_ACCESS_KEY=dummy S3_PUBLIC_BASE_URL=dummy \
              docker compose -f "$f" config -q
          done

  secrets:
    desc: Scan the STAGED DIFF for secrets — the content of the commit being made (gitleaks; skipped if not installed)
    cmds:
      - bash -c 'if command -v gitleaks >/dev/null 2>&1; then gitleaks git --staged -v --no-banner .; else echo "gitleaks not installed — skipping secret scan"; fi'

  secrets:history:
    desc: Scan the whole git history for secrets — catches merge commits and anything committed with --no-verify (runs on pre-push)
    cmds:
      - bash -c 'if command -v gitleaks >/dev/null 2>&1; then gitleaks git . -v --no-banner; else echo "gitleaks not installed — skipping secret scan"; fi'

  build:
    desc: Build all binaries into ./bin
    cmds:
      - go build -ldflags "{{.LDFLAGS}}" -o {{.BIN}}/server ./cmd/server
    vars:
      LDFLAGS:
        sh: 'echo "-X {{.PKG}}/internal/version.Commit=$(git rev-parse --short HEAD 2>/dev/null || echo unknown) -X {{.PKG}}/internal/version.BuildTime=$(date -u +%Y-%m-%dT%H:%M:%SZ)"'

  docker:build:
    desc: "Build the production image as <IMAGE_REGISTRY>liteend:<IMAGE_TAG> (defaults: no registry, latest)"
    vars:
      IMAGE:
        sh: 'echo "${IMAGE_REGISTRY:-}liteend:${IMAGE_TAG:-latest}"'
    cmds:
      - docker build --build-arg COMMIT=$(git rev-parse --short HEAD 2>/dev/null || echo unknown) --build-arg BUILD_TIME=$(date -u +%Y-%m-%dT%H:%M:%SZ) -t {{.IMAGE}} .

  docker:push:
    desc: Push the production image (requires IMAGE_REGISTRY)
    vars:
      IMAGE:
        sh: 'echo "${IMAGE_REGISTRY:-}liteend:${IMAGE_TAG:-latest}"'
    cmds:
      - cmd: |
          if [ -z "${IMAGE_REGISTRY:-}" ]; then
            echo "set IMAGE_REGISTRY (e.g. ghcr.io/acme/) before pushing" >&2
            exit 1
          fi
          docker push {{.IMAGE}}

  lint:
    desc: Run golangci-lint
    cmds:
      - go tool github.com/golangci/golangci-lint/v2/cmd/golangci-lint run

  lint:fix:
    desc: Run golangci-lint with autofixes
    cmds:
      - go tool github.com/golangci/golangci-lint/v2/cmd/golangci-lint run --fix

  arch:
    desc: Verify architecture/layering boundaries (go-arch-lint)
    cmds:
      - go run github.com/fe3dback/go-arch-lint@{{.GO_ARCH_LINT_VERSION}} check

  deadcode:
    desc: Fail if there is unreachable (dead) code anywhere in the program
    cmds:
      # -test treats test files as roots so test-only helpers aren't flagged.
      - cmd: |
          out=$(go run golang.org/x/tools/cmd/deadcode@{{.X_TOOLS_VERSION}} -test ./...)
          if [ -n "$out" ]; then echo "$out"; echo "dead code found — remove it or wire it up"; exit 1; fi

  fmt:
    desc: Format code (gci import order + gofumpt)
    cmds:
      - go tool github.com/golangci/golangci-lint/v2/cmd/golangci-lint fmt
      - go tool mvdan.cc/gofumpt -w .

  fmt:check:
    desc: Fail if any file is not gofumpt-formatted
    cmds:
      - test -z "$(go tool mvdan.cc/gofumpt -l .)" || (go tool mvdan.cc/gofumpt -l . && echo "Unformatted files — run 'task fmt'" && exit 1)

  vuln:
    desc: Scan for known vulnerabilities (Go vuln DB)
    cmds:
      - go tool golang.org/x/vuln/cmd/govulncheck ./...

  test:
    desc: Run unit tests
    cmds:
      - go test -race ./...

  test:watch:
    desc: Re-run unit tests on every .go change (wgo)
    cmds:
      - go tool github.com/bokwoon95/wgo -file '.go$' go test -race ./...

  test:integration:
    desc: Run integration/e2e tests (requires Docker for testcontainers)
    cmds:
      - go test -race -tags=integration ./...

  test:all:
    desc: Run ALL tests — unit + integration via testcontainers (requires Docker) — run before pushing
    cmds:
      - go test -race -count=1 -shuffle=on -tags=integration ./...

  test:cov:
    desc: Run ALL tests with coverage and enforce thresholds (.testcoverage.yml) — requires Docker
    cmds:
      # -coverpkg=./... attributes coverage across packages, so integration
      # tests in test/ count toward the packages they exercise (db, server, …).
      - go test -race -count=1 -shuffle=on -tags=integration -coverpkg=./... -coverprofile=coverage.out ./...
      - go run github.com/vladopajic/go-test-coverage/v2@{{.GO_TEST_COVERAGE_VERSION}} --config=.testcoverage.yml

  db:migrate:
    desc: Apply database migrations with goose (URL built from DATABASE_* in .env)
    dotenv: ['.env']
    cmds:
      - go tool github.com/pressly/goose/v3/cmd/goose -dir db/migrations postgres "postgres://${DATABASE_USER}:${DATABASE_PASSWORD}@${DATABASE_HOST}:${DATABASE_PORT}/${DATABASE_NAME}?sslmode=disable" up

  db:reset:
    desc: Drop and recreate the dev database (DESTROYS local DB data)
    cmds:
      - docker compose down -v db
      - docker compose up -d db
      - sleep 3
      - task: db:migrate

  migration:create:
    desc: "Create a new goose migration (usage: task migration:create name=add_x)"
    requires:
      vars: [name]
    cmds:
      - go tool github.com/pressly/goose/v3/cmd/goose -dir db/migrations create {{.name}} sql

  mod:verify:
    desc: Verify module checksums
    cmds:
      - go mod verify

  update:
    desc: Bump all module + tool deps to latest minor/patch, tidy, regen, then full check (Renovate stays primary)
    cmds:
      - go get -u ./...
      - go get -u tool        # update tool deps from the tool() block in go.mod (Go 1.24+ meta-pattern)
      - go mod tidy
      - task: gen             # sqlc/gqlgen tool versions may have changed — regenerate (gen also runs fmt)
      - task: check           # build + tidy:check + fmt:check + lint + arch + deadcode + vuln + secrets as gate

  tidy:
    desc: Tidy go modules
    cmds:
      - go mod tidy

  tidy:check:
    desc: Fail if go.mod/go.sum are not tidy
    cmds:
      - go mod tidy
      - git diff --exit-code go.mod go.sum || (echo "go.mod/go.sum not tidy — run 'go mod tidy'" && exit 1)

  clean:
    desc: Remove build artifacts and coverage output (keeps ./data)
    cmds:
      - rm -rf {{.BIN}} coverage.out
